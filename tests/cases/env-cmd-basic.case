%% args
env

%% mode
posix-env

%% setenv
FOO=one
API_TOKEN=abcdefghij

%% stdout-cmd
printf '# envctl %s (redacted)\nPATH=/usr/bin:/bin\nFOO=one\nAPI_TOKEN=<redacted>\n' "$(git describe --tags --abbrev --dirty 2>/dev/null || echo unknown)"
